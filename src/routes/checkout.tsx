import { createFileRoute, Link } from "@tanstack/react-router";
import { useState, type FormEvent } from "react";
import { Check, ChevronLeft, CreditCard, MapPin, ShieldCheck, Smartphone, Truck } from "lucide-react";
import { SiteHeader } from "@/components/SiteHeader";
import { useCart } from "@/components/CartProvider";
import { PRODUCTS, TZ_REGIONS, formatTzs } from "@/lib/shop";
export const Route = createFileRoute("/checkout")({ component: CheckoutPage });
const field = "mt-1.5 w-full rounded-lg border border-slate-300 bg-white px-3.5 py-3 text-sm outline-none focus:border-emerald-600 focus:ring-2 focus:ring-emerald-100";
function CheckoutPage() {
  const cart = useCart(); const [region, setRegion] = useState("Dar es Salaam"); const [payment, setPayment] = useState("mpesa"); const [complete, setComplete] = useState(false); const delivery = cart.subtotal >= 100000 && region === "Dar es Salaam" ? 0 : region === "Dar es Salaam" ? 5000 : 9000;
  const submit = (event: FormEvent) => { event.preventDefault(); setComplete(true); cart.clearCart(); window.scrollTo(0, 0); };
  if (complete) return <div className="min-h-screen bg-[#f5f6f7]"><SiteHeader/><main className="mx-auto max-w-2xl px-5 py-20 text-center"><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-emerald-100 text-emerald-700"><Check size={42}/></div><p className="mt-6 text-sm font-bold uppercase tracking-widest text-emerald-700">Order confirmed</p><h1 className="mt-2 text-4xl font-black">Asante! Your order is in.</h1><p className="mx-auto mt-4 max-w-lg leading-7 text-slate-600">We’ve received your order <b>#SS-{Math.floor(100000 + Math.random()*899999)}</b>. You’ll receive an SMS with payment and delivery updates shortly.</p><div className="mt-8 rounded-xl bg-white p-5 text-left text-sm shadow-sm"><b>What happens next?</b><p className="mt-2 text-slate-500">1. We confirm your items with the seller.<br/>2. You receive a payment prompt or delivery confirmation.<br/>3. Track the parcel from your SokoStyle account.</p></div><Link to="/" className="mt-8 inline-block rounded-lg bg-emerald-700 px-7 py-3.5 font-bold text-white">Continue shopping</Link></main></div>;
  if (!cart.items.length) return <div className="min-h-screen bg-[#f5f6f7]"><SiteHeader/><main className="mx-auto max-w-3xl px-5 py-24 text-center"><h1 className="text-3xl font-black">Nothing to check out yet</h1><Link to="/" className="mt-6 inline-block rounded-lg bg-emerald-700 px-6 py-3 font-bold text-white">Shop products</Link></main></div>;
  return <div className="min-h-screen bg-[#f5f6f7]"><SiteHeader/><main className="mx-auto max-w-7xl px-4 py-8"><Link to="/cart" className="flex items-center gap-1 text-sm font-semibold text-emerald-700"><ChevronLeft size={16}/>Back to cart</Link><h1 className="mt-4 text-3xl font-black">Secure checkout</h1><div className="mt-7 grid gap-7 lg:grid-cols-[1fr_380px]"><form id="checkout-form" onSubmit={submit} className="space-y-6"><section className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-emerald-100 text-emerald-700"><MapPin size={19}/></span><h2 className="text-xl font-black">Delivery details</h2></div><div className="mt-6 grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold">Full name<input required className={field} placeholder="e.g. Neema Joseph"/></label><label className="text-sm font-semibold">Phone number<input required type="tel" className={field} placeholder="+255 7XX XXX XXX"/></label><label className="text-sm font-semibold">Region<select value={region} onChange={(e) => setRegion(e.target.value)} className={field}>{TZ_REGIONS.map((r) => <option key={r}>{r}</option>)}</select></label><label className="text-sm font-semibold">District / town<input required className={field} placeholder="Kinondoni"/></label><label className="text-sm font-semibold sm:col-span-2">Street, house and nearest landmark<textarea required rows={3} className={field} placeholder="Mikocheni B, near..."/></label></div></section><section className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-emerald-100 text-emerald-700"><Truck size={19}/></span><h2 className="text-xl font-black">Delivery method</h2></div><label className="mt-5 flex cursor-pointer items-center gap-4 rounded-xl border-2 border-emerald-600 bg-emerald-50 p-4"><input type="radio" checked readOnly/><span className="flex-1 text-sm"><b className="block">Standard tracked delivery</b><span className="text-slate-500">Estimated 1–2 days in Dar, 2–5 days elsewhere</span></span><b>{delivery ? formatTzs(delivery) : "FREE"}</b></label></section><section className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-emerald-100 text-emerald-700"><CreditCard size={19}/></span><h2 className="text-xl font-black">Payment</h2></div><p className="mt-2 text-sm text-slate-500">Choose how you want to pay. Your details are protected.</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{id:"mpesa",label:"M-Pesa",sub:"Vodacom",icon:Smartphone},{id:"airtel",label:"Airtel Money",sub:"Airtel",icon:Smartphone},{id:"mixx",label:"Mixx by Yas",sub:"Yas",icon:Smartphone},{id:"card",label:"Visa / Mastercard",sub:"Debit or credit card",icon:CreditCard},{id:"cash",label:"Cash on delivery",sub:"Available in selected areas",icon:Truck}].map((method) => <label key={method.id} className={`flex cursor-pointer items-center gap-3 rounded-xl border-2 p-4 ${payment === method.id ? "border-emerald-600 bg-emerald-50" : "border-slate-200"}`}><input type="radio" name="payment" value={method.id} checked={payment === method.id} onChange={() => setPayment(method.id)}/><method.icon size={22} className="text-emerald-700"/><span className="text-sm"><b className="block">{method.label}</b><span className="text-xs text-slate-500">{method.sub}</span></span></label>)}</div>{["mpesa","airtel","mixx"].includes(payment) && <label className="mt-5 block text-sm font-semibold">Mobile money number<input required type="tel" className={field} placeholder="07XX XXX XXX"/></label>}</section></form><aside className="h-fit rounded-2xl bg-white p-6 shadow-sm lg:sticky lg:top-40"><h2 className="text-xl font-black">Your order</h2><div className="mt-4 max-h-64 space-y-4 overflow-auto">{cart.items.map((item) => { const p = PRODUCTS.find((x) => x.id === item.productId); return p && <div key={`${item.productId}-${item.size}`} className="flex gap-3 text-sm"><div className="relative h-16 w-12 shrink-0"><img src={p.image_url} className="h-full w-full rounded object-cover" alt=""/><span className="absolute -right-2 -top-2 grid h-5 w-5 place-items-center rounded-full bg-slate-700 text-[10px] text-white">{item.quantity}</span></div><span className="min-w-0 flex-1"><b className="line-clamp-2">{p.name}</b><small className="text-slate-500">{item.color} · {item.size}</small></span><b>{formatTzs(p.price_tzs * item.quantity)}</b></div>;})}</div><dl className="mt-5 space-y-3 border-t pt-5 text-sm"><div className="flex justify-between"><dt>Subtotal</dt><dd>{formatTzs(cart.subtotal)}</dd></div><div className="flex justify-between"><dt>Delivery</dt><dd>{delivery ? formatTzs(delivery) : <span className="text-emerald-700">FREE</span>}</dd></div><div className="flex justify-between border-t pt-4 text-xl font-black"><dt>Total</dt><dd>{formatTzs(cart.subtotal + delivery)}</dd></div></dl><button form="checkout-form" className="mt-6 w-full rounded-lg bg-amber-400 px-5 py-3.5 font-extrabold text-slate-950 hover:bg-amber-300">Place order</button><p className="mt-4 flex items-center justify-center gap-2 text-xs text-slate-500"><ShieldCheck size={15} className="text-emerald-700"/>Encrypted and protected payment</p></aside></div></main></div>;
}
